# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/githubguard.sap_githubteams.yaml
- bases/githubguard.sap_githubs.yaml
- bases/githubguard.sap_githuborganizations.yaml
- bases/githubguard.sap_githubteamrepositories.yaml
- bases/githubguard.sap_ldapgroupproviders.yaml
- bases/githubguard.sap_githubaccountlinks.yaml
- bases/githubguard.sap_genericexternalmemberproviders.yaml
- bases/githubguard.sap_staticmemberproviders.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_githubteams.yaml
#- patches/webhook_in_githubs.yaml
#- patches/webhook_in_githuborganizations.yaml
#- path: patches/webhook_in_githubteamrepositories.yaml
#- path: patches/webhook_in_githubguard.sap_ldapgroupproviders.yaml
#- path: patches/webhook_in_githubguard.sap_githubaccountlinks.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_githubteams.yaml
#- patches/cainjection_in_githubs.yaml
#- patches/cainjection_in_githuborganizations.yaml
#- path: patches/cainjection_in_githubteamrepositories.yaml
#- path: patches/cainjection_in_githubguard.sap_ldapgroupproviders.yaml
#- path: patches/cainjection_in_githubguard.sap_githubaccountlinks.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
