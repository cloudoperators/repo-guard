# SPDX-FileCopyrightText: 2024 SAP SE or an SAP affiliate company and Greenhouse contributors
# SPDX-License-Identifier: Apache-2.0

manager:
  enabled: false
  image:
      repository: ghcr.io/cloudoperators/repo-guard
  replicas: 1
  # Enable leader election. Required when replicas > 1 to prevent race conditions.
  leaderElection: false
  # Resource recommendations based on typical load:
  #   Single-org, low-churn:   requests: cpu=50m,  memory=256Mi  limits: cpu=500m,  memory=512Mi
  #   Multi-org, active sync:  requests: cpu=100m, memory=512Mi  limits: cpu=1000m, memory=1Gi   (default)
  #   High-load (>10 orgs):    requests: cpu=200m, memory=512Mi  limits: cpu=2000m, memory=2Gi
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 100m
      memory: 512Mi
  # Pod Disruption Budget. Enable when replicas > 1 to protect availability during node maintenance.
  pdb:
    enabled: false
    minAvailable: 1

## Global TTL defaults used by templates when org/team-specific overrides are not provided
ttl:
  team:
    failed: "24h"
    notfound: "24h"
    skipped: "24h"
    completed: "72h"
  organization:
    failed: "24h"
    completed: "72h"

monitoring:
  # When enabled, install PrometheusRule alerts for the controller
  enabled: true
  # Default severity label for all rules in this chart (info by default)
  severity: info
  podMonitor:
    enabled: true
    # Optional scrape settings
    interval: 30s
    scrapeTimeout: 10s
    # Additional labels to put on the PodMonitor resource
    # Default includes the kube-monitoring plugin label as requested
    additionalLabels:
      plugin: kube-monitoring

## Resources to be managed by the plugin

# ldaps:
#  - name: 
#    clusterScoped: true
#    host: 
#    baseDN: 
#    bindDN: 
#    bindPW:

# genericExternalMemberProviders:
#  - name: 
#    clusterScoped: true
#    endpoint: 
#    username:
#    password:
#    clientID:
#    clientSecret:
#    # below fields are optional, depending on provider response shape
#    idField: id
#    resultsField: results
#    paginated: false
#    pageParam: page
#    totalPagesField: total_pages
#    testConnectionURL:

# staticMemberProviders:
#  - name: 
#    clusterScoped: true
#    groups:
#      - group: 
#        members:
#          - user1
#          - user2

# githubs:
#   - name: enterprise
#     webURL:
#     v3APIURL:
#     integrationID:
#     clientID:
#     clientSecret:
#     privateKey:

# githubAccountLinks:
#   - github: enterprise
#     userID:
#     githubUsername:
#     githubID:

# githubOrganizations:
#   - github: enterprise
#     organization:
#     installationID:
#     # TTL overrides applied to this organization and all its teams
#     # If omitted, chart-wide defaults in .Values.ttl are used.
#     ttl:
#       failed: 24h        # overrides ttl.team.failed and ttl.organization.failed where applicable
#       notfound: 24h      # overrides ttl.team.notfound
#       completed: 72h     # overrides ttl.team.completed and ttl.organization.completed where applicable
#     addTeam:
#     removeTeam:
#     addOrganizationOwner:
#     removeOrganizationOwner:
#     addRepositoryTeam:
#     removeRepositoryTeam:
#     dryRun:
#     # Opt-in: remove org members that are not in any GitHub team.
#     # Values: "true" (calculate + execute), "dryRun" (calculate only, ops appear in status but not executed), "false"/unset (disabled).
#     removeOrganizationMember:
#     # Opt-in: remove all direct repo collaborators so repository access is managed exclusively through teams.
#     # Values: "true" (calculate + execute), "dryRun" (calculate only, ops appear in status but not executed), "false"/unset (disabled).
#     removeRepositoryDirectCollaborator:
#     # List of GitHub logins that must never be removed by removeOrganizationMember or
#     # removeRepositoryDirectCollaborator. Typically includes bot accounts and the GitHub App user.
#     protectedMembers: []
#     disableInternalUsernames:
#     # GithubAccountLink email verification settings applied to all GALs
#     # associated with this GitHub (organization-level policy).
#     # When set, the Helm chart will populate a JSON annotation on each
#     # GithubAccountLink containing per-organization config entries:
#     #   repo-guard.cloudoperators.dev/email-check-config: '{ "<org>": {"domain": "example.com", "enabled": true, "ttl": "1h" } }'
#     # The controller will write results into:
#     #   repo-guard.cloudoperators.dev/email-check-results: '{ "<org>": {"domain":"example.com","status":"verified|not-part-of-org|no","timestamp":"..."} }'
#     githubAccountLinkEmailCheck:
#       # Domain to require for verified email, e.g., example.com
#       domain: ""
#       # Whether to actively check email verification status
#       enabled: false
#       # Optional TTL for re-checking email status
#       ttl: ""
#
#     defaultPublicRepositoryTeams:
#       - team:
#         permission:
#
#     defaultPrivateRepositoryTeams:
#       - team:
#         permission:
#
#     defaultInternalRepositoryTeams:
#       - team:
#         permission:
#
#     organizationOwnerTeams:
#       -
#
#     teams:
#       - name:
#         greenhouseTeam:
#         # ldap group example referencing top-level ldap provider name:
#         # ldapGroup: cn=my-group,ou=groups,dc=example,dc=org
#         # ldap object example allowing provider and kind override:
#         # ldap:
#         #   provider: corp-ldap   # defaults to .Values.ldaps[0].name if omitted
#         #   kind: ClusterLDAPGroupProvider # optional, auto-populated if provider matches an entry in .Values.ldaps
#         #   group: cn=my-group,ou=groups,dc=example,dc=org
#         # generic http example:
#         # genericHTTP:
#         #   provider: my-http-provider  # must match an entry in genericExternalMemberProviders.name
#         #   kind: ClusterGenericExternalMemberProvider # optional, auto-populated if provider matches an entry in .Values.genericExternalMemberProviders
#         #   group: engineers
#         # static example:
#         # static:
#         #   provider: my-static-provider # must match an entry in staticMemberProviders.name
#         #   kind: ClusterStaticMemberProvider # optional, auto-populated if provider matches an entry in .Values.staticMemberProviders
#         #   group: team-a
#
#     teamRepositoryAssignments:
#       - team:
#         repositories:
#           -
#         permission:
